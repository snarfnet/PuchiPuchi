name: PuchiPuchi
options:
  bundleIdPrefix: com.tokyonasu
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "16.0"

settings:
  base:
    SWIFT_VERSION: "5.9"
    MARKETING_VERSION: "1.0"
    CURRENT_PROJECT_VERSION: "109"
    CODE_SIGN_STYLE: Automatic
    DEVELOPMENT_TEAM: 83VGKGSQUH

targets:
  PuchiPuchi:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - PuchiPuchi
    settings:
      base:
        PRODUCT_NAME: PuchiPuchi
        PRODUCT_BUNDLE_IDENTIFIER: com.tokyonasu.puchipuchi
        TARGETED_DEVICE_FAMILY: "1"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        INFOPLIST_FILE: PuchiPuchi/Resources/Info.plist
    info:
      path: PuchiPuchi/Resources/Info.plist
      properties:
        CFBundleDisplayName: "プチプチ無限"
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        LSRequiresIPhoneOS: true
        LSApplicationCategoryType: public.app-category.entertainment
        ITSAppUsesNonExemptEncryption: false
        UILaunchScreen:
          UIColorName: ""
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
        UIUserInterfaceStyle: Light
        NSUserTrackingUsageDescription: "広告の最適化のため、トラッキングの許可をお願いしています。"
        SKAdNetworkItems:
          - SKAdNetworkIdentifier: cstr6suwn9.skadnetwork
    scheme:
      testTargets: []
